# Catálogo fonte (grande, não deve ir para o repositório) — ver THIRD_PARTY_ASSETS.md
assets/catalogo/raw/
.tmp-audit/

# Dependências npm — só usadas para instalar o GSAP oficial (não-CDN) e copiar
# os builds minificados para js/vendor/gsap/, que é o que a página realmente
# carrega. node_modules/ não precisa ir para o repositório nem ser servido.
node_modules/

# Instaladores e ferramentas locais colocados acidentalmente na pasta do projeto
*.exe
PortableGit/
Git-*.exe
GitHubDesktopSetup*.exe

# Arquivos temporários comuns do Windows e desenvolvimento local
Thumbs.db
Desktop.ini
.DS_Store
*.tmp
*.log

# Saída gerada do graphify (regenerável via `graphify update .`) — ver CLAUDE.md
graphify-out/

# Cache/config local da ferramenta de crítica de design (impeccable)
.impeccable/

# Página de tutorial salva do navegador por engano (não é documentação do projeto)
docs/Integrar Git GitHub Claude Code.html
docs/Integrar Git GitHub Claude Code_files/

# Fotos/vídeos brutos de projetos reais de clientes — não deve ir para o repositório
docs/Photos-1-001/
